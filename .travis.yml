language: python
python:
- "3.12"
deploy:
  provider: gae
  keyfile: $JSON
  key: $private_key
  project name: $project_id
  edge: true
  on:
    branch: main
script:
  - echo "hello world!!!"
  - gcloud -q app deploy app.yaml --project="travis-console-test" --verbosity="warning"

